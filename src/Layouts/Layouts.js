import { BrowserRouter, Route, Switch } from 'react-router-dom';

const Layout = () => {
    return (
        <BrowserRouter>
            <Switch>
                <Route/>
            </Switch>
        </BrowserRouter>
    )
}

export default Layout